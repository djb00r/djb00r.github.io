// Configuration settings for the ice cream maker
const config = {
    // Default settings
    defaultConeType: 'triangle',
    defaultScoopCount: 1,
    defaultFlavor: 'vanilla',
    
    // Animation durations in milliseconds
    servingDuration: 3000,
    
    // Maximum allowed toppings
    maxToppings: 10
};

// Export the config object
export default config;